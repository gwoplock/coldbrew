language: c

script:
  - cd build
  - echo "#!gi homebrew" > test.pkgbuild
  - tar -cvf src.tar ./../src
  - gzip ./src.tar
  - mv ./src.tar.gz ./test.pkg.tar.gz
  - cmake ..
  - make
  - ./coldbrew install --local test.pkg.tar.gz